apiVersion: v1
kind: Service
metadata:
  labels:
    app: helm-basics-training-mobi
  name: helm-basics-training-mobi
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: helm-basics-training-mobi
  sessionAffinity: None
  type: ClusterIP
