apiVersion: v1
kind: Service
metadata:
  labels:
    app: helm-basics-training-rancher
  name: helm-basics-training-rancher
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: helm-basics-training-rancher
  sessionAffinity: None
  type: ClusterIP
