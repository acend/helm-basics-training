apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx-public
    kubernetes.io/tls-acme: "true"
    cert-manager.io/cluster-issuer: acend-letsencrypt
  labels:
    app: helm-basics-training-rancher
  name: helm-basics-training-acend-rancher
spec:
  rules:
  - host: helm-basics-rancher.training.acend.ch
    http:
      paths:
      - backend:
          serviceName: helm-basics-training-rancher
          servicePort: 8080
  tls:

  - hosts:
      - helm-basics-rancher.training.acend.ch
    secretName: helm-basics-rancher-training-acend-ch