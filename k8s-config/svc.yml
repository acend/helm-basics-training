apiVersion: v1
kind: Service
metadata:
  labels:
    app: helm-basics-training
  name: helm-basics-training
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: helm-basics-training
  sessionAffinity: None
  type: ClusterIP
